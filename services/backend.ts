const API_URL = 'https://tkghd.vercel.app';
export const backendAPI = { async health() { // const r = await fetch(`${API_URL}/api/health`); return { success: true, mock: true }; }, async realTransfer(data: any) { // const r = await fetch(`${API_URL}/api/transfer/real`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) }); if (!r.ok) throw new Error('Transfer failed'); return { success: true, mock: true }; }, async getBalance(address: string) { // const r = await fetch(`${API_URL}/api/balance/${address}`); return { success: true, mock: true }; } };
