import express from 'express'; import cors from 'cors'; const app=express(); const PORT=process.env.PORT||8080; app.use(cors({origin:'*'})); app.use(express.json()); app.get('/api/health',(req,res)=>res.json({status:'ok',version:'30.0.0',port:PORT,uptime:process.uptime()})); app.get('/api/zengin/status',(req,res)=>res.json({message:'Zengin OK'})); app.get('/api/real-money/status',(req,res)=>res.json({message:'Real Money OK'})); app.get('/api/legal/jp',(req,res)=>res.json({message:'Legal OK'})); app.get('/api/exchange-rate/USD/JPY',(req,res)=>res.json({rate:150.0})); app.get('/api/revenue',(req,res)=>res.json({total:123456})); app.listen(PORT,()=>console.log(`🚀 Server running on port ${PORT}`));
