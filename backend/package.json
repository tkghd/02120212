{
  "name": "tkgbank-backend",
  "version": "2.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "node -e \"import('pg').then(({default:pg})=>{const pool=new pg.Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});pool.query(require('fs').readFileSync('migrations/001_initial_schema.sql','utf8')).then(()=>console.log('✅ Migration complete')).catch(console.error).finally(()=>pool.end())})\""
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3",
    "qrcode": "^1.5.4",
    "speakeasy": "^2.0.0",
    "stripe": "^20.1.0"
  }
}
